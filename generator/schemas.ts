// The generator validates everything it produces against the same schemas the
// frontend uses to read data, so there is exactly one source of truth for the
// data model. See ../schemas.
export * from "../schemas";
